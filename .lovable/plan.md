De huidige footer neemt veel verticale ruimte in. Deze wijzigingen maken hem compacter:

1. **`src/components/site/Footer.tsx`**
   - Grid-padding verkleinen van `py-12` naar `py-8`
   - Kolom-gap verkleinen van `gap-10` naar `gap-8`
   - Link-spacing in lijsten verkleinen van `space-y-2` naar `space-y-1`
   - Social-icon grootte verkleinen van `h-9 w-9` naar `h-8 w-8`
   - Copyright-balk padding verkleinen van `py-4` naar `py-3`

De inhoud, kolommen, links en kleuren blijven hetzelfde — alleen de ruimtes worden strakker.