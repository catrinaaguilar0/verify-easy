## Plan: Homepagina topmerken-strip aanpassen

### Aanpassingen
1. **Verwijder het woord "Topmerken"** uit de merkenstrip op de homepage (`src/routes/index.tsx`, regel 369).
2. **Maak de merklogo's in kleur** door de `grayscale` en `opacity-80` classes te verwijderen uit de logo-links (`src/routes/index.tsx`, regel 424).

### Technisch
- Wijziging in één bestand: `src/routes/index.tsx`.
- Twee kleine CSS-class aanpassingen, geen nieuwe bestanden of packages nodig.